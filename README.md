
  # Portfolio platform creation

  This is a code bundle for Portfolio platform creation. The original project is available at https://www.figma.com/design/r3NWCDjh6viz409THkirOf/Portfolio-platform-creation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  